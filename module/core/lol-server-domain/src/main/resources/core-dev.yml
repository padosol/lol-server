champion-stats:
  cache:
    enabled: true

spring:
  jackson:
    time-zone: Asia/Seoul

riot:
  api:
    key: ${RIOT_API_KEY}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    prometheus:
      enabled: true
    health:
      show-details: never
  metrics:
    tags:
      application: lol-server

logging:
  level:
    com.example.lolserver.domain.summoner: INFO
    com.example.lolserver.riot: INFO
    com.example.lolserver.aspect: INFO
