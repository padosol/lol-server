server:
  port: 8100

jwt:
  secret-key: ${JWT_SECRET_KEY:local-dev-secret-key-must-be-at-least-256-bits-long-for-hs256}
  access-token-expiry: 1800
  refresh-token-expiry: 1209600

app:
  frontend-callback-url: http://localhost:3000/auth/callback

cors:
  allowed-origins:
    - http://localhost:3000
    - http://localhost:8080
    - http://lol-ui:3000
    - https://metapick.me