global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'lol-server'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['lol-server:8100']
        labels:
          application: 'lol-server'
