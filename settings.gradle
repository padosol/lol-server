rootProject.name = 'lol-server'

include(
        "module:app:application",

        // 공유 모듈
        "module:core:enum",
        "module:support:logging",

        // 공유 커널 (도메인 아님)
        "module:common",

        // ── 수직(컨텍스트별) 도메인 모듈 ──
        "module:domain:leaderboard",
        "module:domain:match",
        "module:domain:championstats",
        "module:domain:gamedata",
        "module:domain:summoner",
        "module:domain:member",
        "module:domain:community",
        "module:domain:duo"
)
